import styled from 'styled-components';

export const StyledPagWrapper = styled.div`
  ul {
    display: flex;
    list-style: none;
  }

  button {
    background-color: #f2a099;
    border: none;
    margin: 2px;
    padding: 10px;
    font-size: 1.2em;
    box-shadow: 0 0 20px rgba(0, 0, 0, 0.15);
  }
`;
